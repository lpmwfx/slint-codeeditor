{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Demo — debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/slint-code-editor.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build (debug)"
    },
    {
      "name": "Demo — release",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/release/slint-code-editor.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build (release)"
    },
    {
      "name": "Demo — åbn aktiv fil",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/slint-code-editor.exe",
      "args": ["${file}"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build (debug)"
    }
  ]
}
